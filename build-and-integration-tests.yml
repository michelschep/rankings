trigger:
  batch: true
  branches:
    include:
      - master

schedules:
- cron: "35 15 * * *"
  displayName: Daily midnight build
  branches:
    include:
    - master
  always: true
      
extends:
  template: devops/base.yml

